# Distinct Column
PAGE.TYPE <- 'child_type'
EVENT.TYPE <- 'action'
REFERRER <- "referer"

SERVICE.TYPE <- NULL

COL.SELECT.TYPES <- c(EVENT.TYPE,PAGE.TYPE) # Columns that can power dropdown, service will be added (maybe)

SITE.SECTION <- 'Section Landing'
TOPIC <- "Topic"
SUB.TOPIC <- "Subtopic"

NON.COL.GROUP <- c(SITE.SECTION, TOPIC, SUB.TOPIC)

CONVERSION.TABLE <- 'conversions'
CONVERSION.DATE <- 'date'

PARENT.TYPE <- 'content_type'

